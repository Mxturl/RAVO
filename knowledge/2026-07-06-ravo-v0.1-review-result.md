# RAVO v0.1 实现评审结果

日期：2026-07-06

## 评审覆盖

- 使用 `model-review-council`
- 原始结果：`/tmp/ravo-v0.1-implementation-review.json`
- coverage：`full_external_review`
- 成功模型：`kimi-k2.7-code`、`qwen3.7-max`
- 失败模型：无

## 评审结论

两个模型均为 `approve_with_changes`。v0.1 范围被认为是合理且足够小的，但需要补强：

- shared manifest 的原子写入
- `AGENTS.md` restore 能力
- 安装文档避免本机硬编码路径
- 官方 plugin validator 需要真实跑通
- fresh thread hook activation 仍需后续人工确认

## 已采纳并修复

- `manifest.json` 和 artifact JSON 写入改为 temp file + rename。
- `ravo-agents.js` 增加 `--restore <backup>`。
- README 安装命令改为从 repo root 执行 `codex plugin marketplace add "$(pwd)"`。
- 使用临时 `/tmp/ravo-pyyaml` 跑通官方 `validate_plugin.py`。
- 重新安装 `ravo-core`、`ravo-analysis`、`ravo-acceptance`，刷新 Codex plugin cache。

## 验证证据

- `PYTHONPATH=/tmp/ravo-pyyaml ... validate_plugin.py plugins/ravo-core`：通过。
- `PYTHONPATH=/tmp/ravo-pyyaml ... validate_plugin.py plugins/ravo-analysis`：通过。
- `PYTHONPATH=/tmp/ravo-pyyaml ... validate_plugin.py plugins/ravo-acceptance`：通过。
- `node scripts/validate-repo.js`：通过。
- `node scripts/smoke-test.js`：通过。
- `ravo-acceptance` empty workspace checker：正确 block。
- `ravo-acceptance` empty workspace hook：正确 block。
- `ravo-acceptance` temp workspace hook with smoke evidence：正确 pass。
- `ravo-agents.js --apply` + `--restore`：通过。
- `codex plugin list`：三个 RAVO 插件均为 `installed, enabled`。

## 残余风险

- 新安装 plugin 的 skills/hooks 通常需要新 Codex thread 才会被当前运行时完整拾取；本轮已验证 cache 文件存在，但未创建新用户线程做 UI 级 hook activation 验证。
- `ravo-acceptance` 目前是最小 JSON artifact 验收，不是完整 release-acceptance 文档体系。
- `ravo-workstream` 和 `ravo-knowledge` 仍是后续版本范围。
