# RAVO v0.2 规格评审记录

日期：2026-07-07

## 输入

- 规格书：`docs/ravo-v0.2-decision-complete-spec.md`
- Goal Prompt：`docs/ravo-v0.2-goal-prompt.md`

## 外部评审状态

`model-review-council` dry-run 通过，预检结果为：

- classification: `internal`
- possible_sensitive_findings: `[]`
- allowed_for_external_review: `true`

正式评审请求已发起，但所有 reviewer model 均返回 HTTP 502 upstream error：

- `MiniMax-M2.5`
- `deepseek-v4-pro`
- `glm-5.2`
- `kimi-k2.7-code`
- `qwen3.7-max`

因此本轮覆盖状态为 `self_review_fallback_required`，不能声称完成 full external review。

Raw result:

- `/tmp/ravo-v0.2-spec-review-dry-run.json`
- `/tmp/ravo-v0.2-spec-review.json`

## 本地自审修正

发现一个执行歧义：规格中要求“检查是否已有 decision-complete spec”，但没有定义 discovery order 和判定规则。

已修正：

- 在规格书中新增 spec discovery order。
- 在规格书中新增 Goal authoring validation scenarios。
- 在 Goal Prompt 完成标准中加入 Goal Prompt Authoring 行为。

## 残余风险

- 仍需在外部评审服务恢复后重跑 `model-review-council`。
- 当前规格可作为实现输入，但外部审查状态只能标记为 self-review fallback。
